# Meckano Reporter

## Setup

### Dependencies

1. Node.js (latest LTS version)
2. Copy `.env.example` to `.env` and define the required environment variables.

### Installation

```bash
npm install
npm run report
```

